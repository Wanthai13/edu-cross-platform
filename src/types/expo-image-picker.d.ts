declare module 'expo-image-picker';

// Minimal ambient declaration to silence TypeScript when the package isn't installed yet.
// Replace by proper types (remove this file) after `npm install expo-image-picker` completes.
